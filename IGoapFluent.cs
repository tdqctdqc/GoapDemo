using Godot;
using System;

public interface IGoapFluent
{
    // string Name { get; }
    // Type ValueType { get; }
    // Type AgentType { get; }
    // bool SatisfiedBy(IGoapState state);
    // object GetValue();
}
